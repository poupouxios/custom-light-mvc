<div class="row bottom-buffer">
	<div class="col-sm-12">
		<?php if(strlen($this->message) > 0): ?>
			<div class="alert alert-success">
				<?= $this->message ?>
			</div>
		<?php endif; ?>
		<?php if(strlen($this->warningmessage) > 0): ?>
			<div class="alert alert-danger">
				<?= $this->warningmessage ?>
			</div>
		<?php endif; ?>
		<form class="form-signin" action="/saving/edit/<?= $this->saving->id ?>" method="post">
      <h2 class="form-signin-heading">Edit Saving <?= $this->saving->id ?></h2>

			<div class="form-group">
		    <label for="saving_value">Saving value</label>
				<div class="input-group">
				  <span class="input-group-addon" id="sizing-addon1">&euro;</span>
			    <input type="number" step="0.01" min="0.00" id="saving_value" value="<?= $this->saving->saving_value ?>" name="saving_value" class="form-control" placeholder="eg. 9.03" required="" autofocus="">
				</div>
			</div>

			<div class="form-group">
		    <label for="comment">Comment</label>
		    <textarea row="5" col="5" id="comment" name="comment" class="form-control" required="" autofocus=""><?= $this->saving->comment ?></textarea>
			</div>

			<div class="form-group">
		    <label for="user_id">User</label>
				<select name="user_id" class="form-control" required>
					<?php foreach($this->users as $user): ?>
						<?php if($this->saving->user_id == $user->id): ?>
							<option value="<?= $user->id ?>" selected><?=$user->firstname ?> <?= $user->surname ?></option>
						<?php else: ?>
							<option value="<?= $user->id ?>"><?=$user->firstname ?> <?= $user->surname ?></option>
						<?php endif; ?>
					<?php endforeach; ?>
				</select>
			</div>

      <button class="btn btn-primary" type="submit">Update</button>
    </form>
	</div>
</div>
